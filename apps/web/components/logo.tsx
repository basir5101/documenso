import Link from "next/link";
import { classNames } from "@documenso/lib";

export default function Logo(props: any) {
  return (
      <img className="mx-auto h-20 w-auto text-black" src="/logo.png" height={'40px'} width={'200px'} alt="sign now" />

  );
}
